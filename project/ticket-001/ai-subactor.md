# Subactor execution plan — ticket-001

- State: PUBLICATION
- Workstream: integration
- Worktree: dedicated sibling checkout `sop-ticket-001-todo`
- Branch: `ticket-001/todo-completion`
- Base HEAD: `31f7fc9ec8cca80c0a867e50c75737d8a0a1ba06`

## SESSION_EXECUTION_AUTHORIZATION

The user's requests in this session explicitly authorize autonomous implementation of unfinished ticket-001 TODO work in the dedicated worktree. Authority includes local source, specification, tests, documentation, and ticket evidence within `intent.json` allowedPaths. The user's subsequent instruction to push according to the repository standard authorizes commit, branch push, pull-request creation, and invocation of the declared protected validator-agent delivery process. It excludes direct merge, external model benchmark runs, secret access, edits to forbidden paths, and any publication path that bypasses trusted validation.

## Plan and result

1. Audit declarations against code and establish test/governance baseline.
2. Align schema, validator, catalog, and the foundation `sop-new-ticket` procedure.
3. Implement deterministic local scanner/diff/patch/sync/verify with safe dry-run and path controls.
4. Add local-only integration command contracts and comprehensive tests.
5. Repair architecture and define reproducible Dual-LLM methodology/scenarios/prompts.
6. Split publication into a three-public-file foundation slice; defer the other procedures and keep external execution blocked.

The foundation implementation is complete. Exact managed bytes were restored from the index after a clean-path preflight and verified against the lock. Ticket is `IN_PROGRESS / PUBLICATION` for exact-head review and trusted delivery; the dependent procedures are pending a later ticket, and external benchmark evaluations were not executed.
